
# 💻 Day 7 - Python Sets





## 📘 Level 1

1. **Find the length of the set `it_companies`:**
2. **Add `'Twitter'` to `it_companies`:**
3. **Insert multiple IT companies at once to the set `it_companies`:**
4. **Remove one of the companies from the set `it_companies`:**
5. **Difference between `remove` and `discard`:**\
  `remove()` will raise an error if the item does not exist in the set.
  `discard()` will not raise an error if the item does not exist.

---

## 📗 Level 2

1. **Join `A` and `B`:**
2. **Find `A` intersection `B`:**
3. **Is `A` subset of `B`?**
4. **Are `A` and `B` disjoint sets?**
5. **Join `A` with `B` and `B` with `A`:**
6. **Symmetric difference between `A` and `B`:**
7. **Delete the sets completely:**

---

## 📕 Level 3

1. **Convert the `age` list to a set and compare lengths:**
2. **Difference between data types:**\
  **String:** A sequence of characters, immutable, e.g., `'hello'`.\
  **List:** Ordered, mutable collection of items, e.g., `[1, 2, 3]`.\
  **Tuple:** Ordered, immutable collection of items, e.g., `(1, 2, 3)`.\
  **Set:** Unordered collection of **unique** items, mutable, e.g., `{1, 2, 3}`.

3. **Find unique words in a sentence:**
```python
sentence = "I am a teacher and I love to inspire and teach people"
```
---
**Gurmad Harir**  
January 12, 2026
